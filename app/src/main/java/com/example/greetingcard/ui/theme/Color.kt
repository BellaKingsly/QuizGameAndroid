package com.example.greetingcard.ui.theme

import androidx.compose.ui.graphics.Color

// game ui colors (used across app)

// main action color (buttons, start, restart)
val GameOrange = Color(0xFFFF8A00)

// success / coins / correct answer
val GameGreen = Color(0xFF4CAF50)

// background color
val GameBackground = Color(0xFFFFF8E1)

// error / wrong answer
val GameRed = Color(0xFFE53935)

// white surface / cards
val GameWhite = Color(0xFFFFFFFF)